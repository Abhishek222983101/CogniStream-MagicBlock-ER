--- api.ts
+++ api.ts
@@ -107,13 +107,24 @@
   return {
     patient_id: (raw.patient_id as string) || `UPLOAD_${Date.now()}`,
     demographics: {
       age: raw.age || raw.demographics,
-      gender: raw.gender || "Unknown",
-      name: raw.name,
-      city: raw.city,
-      state: raw.state,
-      lat: raw.lat,
-      lng: raw.lng,
+      gender: typeof raw.demographics === "object" && raw.demographics !== null && "gender" in raw.demographics 
+          ? (raw.demographics as any).gender 
+          : raw.gender || "Unknown",
+      name: typeof raw.demographics === "object" && raw.demographics !== null && "name" in raw.demographics 
+          ? (raw.demographics as any).name 
+          : raw.name,
+      city: typeof raw.demographics === "object" && raw.demographics !== null && "city" in raw.demographics 
+          ? (raw.demographics as any).city 
+          : raw.city,
+      state: typeof raw.demographics === "object" && raw.demographics !== null && "state" in raw.demographics 
+          ? (raw.demographics as any).state 
+          : raw.state,
+      lat: typeof raw.demographics === "object" && raw.demographics !== null && "lat" in raw.demographics 
+          ? (raw.demographics as any).lat 
+          : raw.lat,
+      lng: typeof raw.demographics === "object" && raw.demographics !== null && "lng" in raw.demographics 
+          ? (raw.demographics as any).lng 
+          : raw.lng,
     },
     diagnosis: {
-      primary: raw.diagnosis || raw.primary_diagnosis || "Unknown",
